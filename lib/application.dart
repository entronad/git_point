import 'package:fluro/fluro.dart';

class Application {
  // All routes defined in this router instance for the convenience to use Application.router.navigateTo().
  static final Router router = Router();
}
